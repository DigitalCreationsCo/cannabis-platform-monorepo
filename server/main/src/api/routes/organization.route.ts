import { Router } from "express";
import { orgCtrl } from "../controllers";
const router = Router();
/* =================================
Organization Routes

members:
================================= */

export default router;
