import { Router } from "express"
import { userCtrl } from "../controllers"
const router = Router()
/* =================================
User Routes

================================= */

export default router