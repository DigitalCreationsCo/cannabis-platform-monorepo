apiVersion: gateway.solo.io/v1
kind: RouteTable
metadata:
  name: gras-dashboard
  namespace: gloo-system
  labels:
    domain: 'app.grascannabis.org'
spec:
  routes:
    - matchers:
        - prefix: /
      routeAction:
        single:
          upstream:
            name: default-gras-dashboard-8080
            namespace: gloo-system
