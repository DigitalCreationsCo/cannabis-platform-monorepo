---
apiVersion: v1
kind: Service
metadata:
  name: gras-server-image
spec:
  type: ClusterIP
  selector:
    app: gras-server-image
  ports:
    - protocol: TCP
      port: 8080
      targetPort: image-port
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gras-server-image
  name: gras-server-image
  namespace: default
spec:
  selector:
    matchLabels:
      app: gras-server-image
  replicas: 1
  template:
    metadata:
      labels:
        app: gras-server-image
    spec:
      serviceAccountName: default
      restartPolicy: Always
      containers:
        - name: gras-server-image
          image: grasadmin/image:TAG
          imagePullPolicy: Always
          terminationMessagePolicy: FallbackToLogsOnError
          resources: 
            requests:
              cpu: '0.1'
              memory: '128Mi'
            limits:
              cpu: '0.2'
              memory: '256Mi'
          ports:
            - name: image-port
              containerPort: 6031
          livenessProbe:
            httpGet:
              path: /api/v1/healthcheck
              port: image-port
            failureThreshold: 2
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /api/v1/healthcheck
              port: image-port
            failureThreshold: 2
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3