FROM --platform=linux/amd64 node:16-alpine as install
COPY package.json ./
COPY yarn.lock ./
COPY .yarnrc.yml ./
COPY .yarn ./.yarn
RUN yarn --immutable

FROM --platform=linux/amd64 node:16-alpine as node_modules
COPY --from=install /node_modules ./node_modules