{
  "name": "@cd/delivery-widget",
  "private": true,
  "version": "0.0.1",
  "main": "./src",
  "scripts": {
    "build": "yarn build:clean && tsc && yarn bundle:ejs",
    "build:clean": "rm -rf dist",
    "bundle:ejs": "copyfiles -u 3 \"./src/**/*.ejs\" \"./dist/widget/views\" --verbose",
    "start": "yarn build && node dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ejs": "3.1.9",
    "express": "4.18.2",
    "typescript": "4.9.3"
  },
  "devDependencies": {
    "@types/ejs": "3.1.2",
    "copyfiles": "^2.4.1"
  }
}
