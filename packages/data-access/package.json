{
  "name": "@cd/data-access",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "./index.ts",
  "module": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "prisma": {
    "seed": "node seed.js"
  },
  "scripts": {
    "postinstall": "prisma generate",
    "generate": "prisma generate",
    "dev": "pscale connect cannabis_delivery_v1 dev --port 3309",
    "migrate": "prisma migrate",
    "push": "prisma db push",
    "deploy": "pscale deploy-request create cannabis_delivery_v1 dev",
    "seed": "prisma generate && prisma db push && prisma db seed",
    "studio": "prisma studio"

  },
  "peerDependencies": {
    "@prisma/client": "4.4.0",
    "prisma": "4.4.0"
  }
}
