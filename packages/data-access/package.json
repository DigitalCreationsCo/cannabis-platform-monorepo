{
  "name": "@cd/data-access",
  "private": true,
  "version": "0.0.1",
  "main": "./index.ts",
  "prisma": {
    "seed": "ts-node --esm seed.ts"
  },
  "scripts": {
    "dev": "tsc --watch",
    "generate": "prisma generate",
    "connect": "pscale connect cannabis_delivery_v1 dev --port 3309",
    "migrate": "prisma migrate",
    "push": "prisma db push",
    "deploy": "pscale deploy-request create cannabis_delivery_v1 dev",
    "seed": "prisma generate && prisma db push && prisma db seed",
    "studio": "prisma studio"
  },
  "peerDependencies": {
    "@prisma/client": "4.4.0",
    "prisma": "4.4.0",
    "ts-node": "10.9.1",
    "typescript": "4.9.3"
  }
}
