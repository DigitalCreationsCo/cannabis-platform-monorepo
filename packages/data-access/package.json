{
  "devDependencies": {
    "@cd/shared-config": "*",
    "@prisma/client": "4.4.0",
    "@types/jest": "29.2.4",
    "jest": "29.3.1",
    "jest-mock-extended": "3.0.1",
    "prisma": "4.4.0",
    "ts-jest": "29.0.3",
    "ts-node": "10.9.1",
    "typescript": "4.9.3"
  },
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "name": "@cd/data-access",
  "prisma": {
    "schema": "./schema.prisma",
    "seed": "ts-node --esm src/db/seed.ts"
  },
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "clear": "ts-node --esm src/db/clear.ts",
    "connect": "pscale connect cannabis_delivery_v1 dev --port 3309",
    "deploy": "pscale deploy-request create cannabis_delivery_v1 dev",
    "dev": "tsc --watch src/index.ts",
    "generate": "prisma generate",
    "migrate": "prisma migrate",
    "push": "prisma db push",
    "reseed": "prisma generate && prisma db push && yarn db clear && prisma db seed",
    "seed": "prisma db seed",
    "studio": "prisma studio",
    "test": "../../node_modules/jest/bin/jest.js"
  },
  "version": "0.0.1"
}
