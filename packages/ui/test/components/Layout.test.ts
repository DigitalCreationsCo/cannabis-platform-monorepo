/* eslint-disable jest/no-commented-out-tests */
import { Layout } from '../../src/components';

describe('Layout Tests', () => {
	it('should be defined', () => {
		expect(Layout).toBeDefined();
	});
	// it('should require a TopBarComponent prop', () => {});
	// it('with showTopBar: true, the TopBar is visibile', () => {});
	// it('with showTopBar: false, the TopBar is not visibile', () => {});
	// it('with showSideNav: true, the sidenav is  visibile', () => {});
	// it('with showSideNav: false, the sidenav is not visibile', () => {})
	// it('with showHeader: true, the header is visibile', () => {});
	// it('with showHeader: false, the header is not visibile', () => {});
});
