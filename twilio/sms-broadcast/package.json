{
    "name": "sms-broadcast",
    "devDependencies": {
        "eslint-plugin-prettier": "4.2.1",
        "prettier": "3.2.5"
    },
    "private": true,
    "version": "0.1.0",
    "scripts": {
        "deploy": "twilio serverless:deploy --ui-editable",
        "update": "twilio serverless:deploy --override-existing-project --ui-editable"
    }
}
