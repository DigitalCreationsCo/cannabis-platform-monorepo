private_key_file="terraform/certs/privkey.pem"
certificate_file="terraform/certs/fullchain.pem"

echo "This command does not work. Please see the file terraform/scripts for the correct commands to run."
# echo "Generate new cert request file"
# echo "with private key ${private_key_file}"
# openssl req -new -key $private_key_file -out new_cert.csr